<Window x:Class="SentinelCore.UI.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, RelativeSource={RelativeSource Self}, Mode=OneWay}"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource ChatBackground}"
        WindowStyle="SingleBorderWindow">

    <Border Padding="24,20"
            BorderBrush="{DynamicResource ChatBorder}"
            BorderThickness="1"
            CornerRadius="8"
            Margin="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Message body -->
            <TextBlock x:Name="MessageText"
                       Grid.Row="0"
                       TextWrapping="Wrap"
                       FontSize="13"
                       Foreground="{DynamicResource ChatTextPrimary}" />

            <!-- Buttons -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0">
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,12,0"
                        IsCancel="True"
                        Click="OnCancelClicked" />
                <Button x:Name="ConfirmButton"
                        Style="{StaticResource AccentButtonStyle}"
                        IsDefault="True"
                        Click="OnConfirmClicked" />
            </StackPanel>
        </Grid>
    </Border>
</Window>